# Used by Prisma, `npm run dev`, and `npm run db:load_data` (see `config/game.json`).
# With Docker Compose from the repo root, match ../.env (POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB) and use host localhost:5432.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
PORT=5001

