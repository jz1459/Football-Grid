# Docker Compose reads this file as `.env` in the repository root (copy this file to `.env`).
# Do not commit `.env`. Keep `backend/.env` `DATABASE_URL` in sync when loading data from the host
# (same user, password, database name; host `localhost`, port `5432`).

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=database
